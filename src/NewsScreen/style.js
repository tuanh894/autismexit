import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
   container: {
       backgroundColor: '#FFF',
   },
    content: {
       padding: 15
    },
    title: {
       fontWeight: 'bold',
        fontSize: 18,
        paddingBottom: 10,
        textAlign: 'center'
    },
});

export default styles;